schema_version: 1
track_id: TRACK-B
name: Complete Experience and Delivery
status: IN_PROGRESS
implementation_audit_base: 97500a385a1fa1a5f0e4c3bbb6e2c1b0c895ec61
minimum_handoff_commit: 7e0688308292cd9d41552c98f576d25681738be0
base_sha: 14e1b85596034544b7fb291677fb8dd3f8240f1c
branch: codex/track-b-complete-experience
owner: claude-code-track-b
updated_at: "2026-07-19"
spec: docs/tracks/TRACK-B-COMPLETE-EXPERIENCE.md
evidence_root: evidence/track-b
integration_requests: docs/goal/tracks/track-b-integration-requests.yml
current_front: B4-i18n-a11y
continuation_base:
  branch: feat/complete-product-continuation
  base_sha: 85b3a5b30519cc5894c6ee4a94fe9844d20bc3ee
  note: >
    Teammate continuation branched from latest origin/main (85b3a5b). Did NOT cherry-pick
    7f1aa9b and did NOT re-implement the merged A3 vector foundation or B1/PWA checkpoints.
handoff_checkpoint:
  status: IN_PROGRESS
  scope: "Second domain-hook extraction plus explicit PWA install/update UX; this checkpoint supersedes older B1/B5 remaining text where they conflict."
  accepted_changes:
    - "web/src/hooks/useConnectionsAndLetters.ts extracts the connections, relations, people discovery, slow-letter inbox and reply lifecycle from AuroraApp.tsx."
    - "InstallPrompt, PwaUpdateNotice and UpdateBanner add a user-controlled install and update flow; registerType is prompt, so an active Aurora turn is not silently reloaded."
    - "Generated Spring static assets were rebuilt from the reviewed React source."
  verification:
    frontend_tests: "30 files, 199 tests, 0 failed."
    frontend_build: "npm run build PASS; Vite PWA generateSW emitted 18 precache entries and workbox-98f7a950.js."
    whitespace: "git diff --check PASS."
  remaining:
    - "AuroraApp.tsx still owns memory/profile, capsule/resonance, psychology-skill, portrait and account domains; continue decomposition by cohesive domain, not line-count-only extraction."
    - "Install affordance remains browser/platform dependent and needs real-device validation; update flow needs a real deployed-version rehearsal during an active and idle Aurora turn."
    - "Nested routes, progressive disclosure, per-item busy identifiers, localized offline copy, Capacitor/device gates, B2-B4 and B6-B7 remain open."
    - "TB-REQ-001 and TB-REQ-002 remain cross-track integration requests."
workstreams:
  B0-experience-observation:
    status: DONE
    evidence:
      - evidence/track-b/README.md
      - evidence/track-b/experience-inventory.md
      - evidence/track-b/golden-journeys.md
      - evidence/track-b/screenshots/
      - evidence/track-b/scripts/observe-b0-journeys.mjs
    remaining: "Live-verified only J1 (partially), the Aurora conversation itself, offline reload, and expired-auth deep link. J2 (WakeIntent return), most of J3 (capsule compile/sandbox/match-chat/slow-letter), provider-unavailable, denied-permission, partial-stream-reconnect, conflicting-correction and withdrawn-data recovery paths are still UNVERIFIED live (see golden-journeys.md verification tags) -- retest as B1/B2 build the real flows, do not assume DONE means fully exercised."
  B1-product-architecture:
    status: IN_PROGRESS
    evidence:
      - web/src/components/AuthGate.tsx
      - web/src/components/AuthGate.test.tsx
      - web/src/api.ts (api.register)
      - evidence/track-b/scripts/observe-b1-register.mjs
      - evidence/track-b/screenshots/b1-01-*.png .. b1-12b-*.png
      - web/src/components/ProductShell.tsx (spacePath, productSpaceFromPath)
      - web/src/main.tsx (HashRouter)
      - evidence/track-b/scripts/observe-b1-routes.mjs
      - evidence/track-b/screenshots/b1-routes-*.png
      - docs/goal/tracks/track-b-integration-requests.yml (TB-REQ-002)
      - web/src/components/MemoryStarfield.tsx (AsyncButton adoption: importance save, archive, reveal-star, rollback)
      - web/src/components/ClaimCandidateReview.tsx (AsyncButton adoption: confirm, dismiss-confirm)
      - web/src/components/CapsuleWorkbench.tsx (AsyncButton adoption: boundary save)
      - web/src/components/UnderstandingCorrection.tsx (AsyncButton adoption: retire-confirm)
      - web/src/components/PsychologySkillStudio.tsx (AsyncButton adoption: skill-run)
      - web/src/components/RelationsView.tsx (LoadingText adoption: relation-timeline busy panel)
      - web/src/components/AuthGate.tsx (AsyncButton adoption: login/register submit)
      - web/src/components/AuroraConversation.tsx (AsyncButton adoption: voice/mic button)
      - web/src/components/PortraitView.tsx (AsyncButton adoption: calibrate button)
      - evidence/track-b/scripts/observe-b1-loading-audit.mjs
      - evidence/track-b/screenshots/b1-loading-audit-*.png
      - evidence/track-b/README.md ("Workstream B1" sections)
      - web/src/hooks/useAuroraSession.ts (new: Aurora conversation/session domain hook)
      - web/src/hooks/useAuroraSession.test.ts (14 new Vitest cases characterizing the extracted domain)
      - web/src/AuroraApp.tsx (calls useAuroraSession(); JSX/effects updated to consume it, no behavior change)
      - evidence/track-b/scripts/observe-b1-decompose-aurora.mjs
      - evidence/track-b/screenshots/b1-decompose-01..05*.png, observation-log-b1-decompose-aurora.txt
    remaining: "Five of B1's items now done: (1) unified login/register entry surface, (2) Aurora composer reachable without scrolling on mobile+desktop, (3) real client routes (react-router HashRouter) replacing the ?space= query param, (4) single async/task/error model audit, (5) FIRST domain-hook decomposition slice -- the Aurora conversation/session domain (message list, streaming/turn status, interrupt/stop, mode picker, WakeIntent negotiate, session bootstrap/replay) extracted out of AuroraApp.tsx into web/src/hooks/useAuroraSession.ts. Still open: AuroraApp.tsx is down from ~1270 to ~900 lines but still holds every OTHER domain's state as inline useState (memory/starfield, correction/claims, capsule/resonance, connections/letters, psychology skills, portrait, account) -- none of those moved this pass. Several more disabled={busy}-only buttons were deliberately left as documented discoveries (see prior checkpoint) because they share one busy boolean across sibling buttons representing different items (AuroraSelfSpace's propose/evaluate/activate/rollback, MemoryStarfield's mode tabs, ResonanceNetwork's strategy switcher, PsychologySkillStudio's per-run revoke, CapsuleWorkbench's sandbox ratings) -- migrating those safely needs a per-item busy id, which is domain-hook-decomposition-shaped work for whichever domain is extracted next, not a call-site swap; LettersInbox's reply button has no busy tracking at all (a real gap, flagged not fixed). No progressive-disclosure pass on WakeIntent/Self-Emergence panels, and only the five top-level spaces have real routes (nested sub-routes like /cosmos/starfield vs /cosmos/portrait, /resonance/capsule/:id are not yet split out -- productSpaceFromPath already matches nested sub-paths so this is additive, not a rework). HashRouter (not BrowserRouter) is used because Spring's static handling has no /app/aurora/** catch-all -- filed as TB-REQ-002 for a future clean-URL swap."
  B2-visible-innovation: {status: NOT_STARTED, evidence: [], remaining: "Make living Aurora, Inner Cosmos and resonance differentiation immediately visible."}
  B3-design-content: {status: NOT_STARTED, evidence: [], remaining: "Complete design primitives, content states, responsive polish and visual regression."}
  B4-i18n-a11y:
    status: IN_PROGRESS
    evidence:
      - web/src/i18n/messages.ts
      - web/src/i18n/locale.ts
      - web/src/i18n/LocaleProvider.tsx
      - web/src/i18n/messages.test.ts
      - web/src/i18n/locale.test.ts
      - web/src/components/LanguageToggle.tsx
      - web/src/components/LanguageToggle.test.tsx
      - web/src/components/ProductShell.tsx (ProductShellNavigation + MeSpace migrated to catalog; productSpaces -> PRODUCT_SPACES)
      - web/src/components/ProductShell.test.tsx (zh-CN preserved + en-SG nav case)
      - web/src/components/AuthGate.tsx (migrated to catalog)
      - web/src/components/AuthGate.test.tsx (zh-CN characterization preserved + en-SG cases)
      - web/src/main.tsx (LocaleProvider wraps the app)
      - web/src/AuroraApp.tsx (legacy-redirect updated for the PRODUCT_SPACES rename)
      - evidence/track-b/B4-i18n/README.md
    remaining: >
      FIRST B4 slice: established the i18n foundation (a TYPED message catalog with an
      explicit Messages interface so a missing key/locale is a compile error; zh-CN + full
      en-SG; count phrases as per-locale functions with English singular/plural) plus a
      LocaleProvider/useMessages/useLocale context, browser-language detection
      (zh*->zh-CN, en*->en-SG English-first), localStorage persistence (ic-locale, mirrors
      theme.ts) and <html lang> application, and a LanguageToggle in the Me space. Migrated
      the ENTRY SURFACE only: AuthGate (first screen every new user sees) and
      ProductShellNavigation + MeSpace. These now switch 中文/English instantly with no
      reload. Still open: migrate the remaining spaces (Aurora conversation, Inner
      Cosmos/starfield, Resonance, Connections/letters, psychology-skill chrome, account
      settings) onto the same Messages contract (most hard-coded Chinese still lives there);
      localise dates/timezone/relative-time/numbers/validation/notifications/safety
      resources; en-SG copy review by a fluent reviewer (human gate); and the entire WCAG
      2.2 AA + axe/keyboard/screen-reader half of B4 (NOT started this slice).
    verification:
      environment_gap: >
        ENVIRONMENT BLOCKER (recorded honestly, not hidden): this continuation ran in a
        sandbox with no JDK 21 and no Maven Central access (Track A unbuildable) AND where
        `npm install` cannot complete through a throttled package proxy (~37s/request; long
        installs are killed), so Vitest, `tsc -b` and `vite build` COULD NOT be run here.
        This checkpoint is therefore IMPLEMENTED, not a green-suite PASS. The full frontend
        gate must be run on a real dev machine (see evidence/track-b/B4-i18n/README.md §5)
        before this is upgraded past IMPLEMENTED.
      core_executed: >
        Dependency-free core WAS executed for real via Node 22 TypeScript type-stripping
        (not self-scoring): `node` over web/src/i18n/messages.ts asserted identical deep
        key shape across both locales, no empty-string leaves, correct count interpolation
        incl. English singular/plural, and default-locale fallback -> MESSAGES_OK. `node`
        over web/src/i18n/locale.ts asserted detectLocale zh*/en*/other + ordering,
        silent storage degradation without localStorage, resolveInitialLocale validity, and
        applyDocumentLocale -> LOCALE_OK.
      static_review: >
        Cannot compile the React/TSX here. Manual audit: no user-visible hard-coded Chinese
        remains in the migrated AuthGate.tsx/ProductShell.tsx (comments only); all new
        imports resolve to real files; no runtime import cycle (only an erased `import type`
        edges back to ProductShell); old `productSpaces` export has zero remaining refs;
        `git diff --cached --check` (whitespace) PASS.
      component_tests_written_not_run: >
        messages.test.ts, locale.test.ts, LanguageToggle.test.tsx, and the extended
        AuthGate.test.tsx / ProductShell.test.tsx are written (TDD-style, zh-CN
        characterization preserved + en-SG cases added) but require `npm i` to run.
      secret_scan: >
        scripts/scan-secrets.ps1 needs PowerShell, absent in this sandbox. The staged diff
        is UI strings + i18n plumbing only, with zero credentials added; run the scan on a
        real machine before pushing.
  B5-pwa-mobile:
    status: IN_PROGRESS
    evidence:
      - web/vite.config.ts (VitePWA plugin config: generateSW strategy, manifest, navigateFallback/denylist, explicit NetworkOnly runtimeCaching for /api/**)
      - web/src/pwaManifest.ts (buildPwaManifest(): testable pure function for the web app manifest)
      - web/src/pwaManifest.test.ts (6 Vitest cases pinning the manifest's shape)
      - web/src/main.tsx (registerSW from virtual:pwa-register, guarded by "serviceWorker" in navigator)
      - web/src/vite-env.d.ts (/// <reference types="vite-plugin-pwa/client" />)
      - web/tsconfig.node.json (includes src/pwaManifest.ts so vite.config.ts can import it across the app/node project split; noEmit added to stop tsc -b from writing stray .js/.d.ts next to the source)
      - web/scripts/generate-pwa-icons.mjs (one-time/re-runnable icon generator; reuses ios/App/App/Assets.xcassets/AppIcon.appiconset/AppIcon-512@2x.png via a headless-Chromium canvas resize, no new artwork, no new image-processing dependency)
      - web/public/icons/icon-192.png, icon-512.png, icon-512-maskable.png (generated by the script above)
      - src/main/resources/static/app/aurora/manifest.webmanifest, sw.js, workbox-abeb32eb.js, assets/workbox-window.prod.es5.js, icons/ (built output)
      - evidence/track-b/scripts/observe-b1-pwa-offline.mjs
      - evidence/track-b/screenshots/b1-pwa-offline-01..04*.png, observation-log-b1-pwa-offline.txt
    remaining: "This is the FIRST B5 slice only: the offline app shell (manifest + precache + branded offline fallback + the API-caching boundary). Still open for later B5 slices: (1) an actual install-prompt UX (beforeinstallprompt capture + an in-app 'install' affordance -- currently the browser's own default install UI is all that exists); (2) a real versioned-update flow (registerType is 'autoUpdate' today, which silently reloads on a new deploy with zero user-visible copy or confirmation -- no onNeedRefresh/onOfflineReady UI was built); (3) Capacitor-specific checks from spec section 9 (deep links, secure storage, network/background transitions, microphone/push-permission UX) -- untouched this pass, this slice is web-PWA only; (4) the raw 'Failed to fetch' browser-native error text inside the branded ConnectError card (see discoveries) -- a real, small copy gap, deliberately not fixed to avoid touching AuroraApp.tsx/api.ts while a parallel agent is mid-refactor there; (5) the truly-never-visited-before offline case (SW not yet installed/no cache at all) still shows the browser's native blank error page -- only verified/fixed the 'visited online once, then offline' case the task specified."
  B6-local-complete: {status: NOT_STARTED, evidence: [], remaining: "Make the complete real-provider product reproducible locally from a clean checkout."}
  B7-demo-handoff: {status: NOT_STARTED, evidence: [], remaining: "Deliver full E2E, 8-12 minute demo and non-author product/operations handoff."}
latest_checkpoint: "B1 fourth checkpoint: first AuroraApp.tsx domain-hook decomposition slice. Extracted the Aurora conversation/session domain -- message list, streaming/turn status, interrupt/stop, mode picker, WakeIntent negotiate, and session bootstrap/replay -- out of the ~1270-line, 60+-useState AuroraApp.tsx into a dedicated web/src/hooks/useAuroraSession.ts, following docs/tracks/TRACK-B-COMPLETE-EXPERIENCE.md section 5's decomposition ask. Pure state/logic move: no JSX/rendering structure or visible-behavior change. AuroraApp.tsx now calls the hook and passes its returned state/handlers straight through to the same JSX it always rendered. TDD'd first: 14 new characterization tests in useAuroraSession.test.ts (renderHook + mocked api/streamAurora/replayTurnEvents/subscribeProactive) pin session bootstrap/replay, send/stream/interrupt, mode switching and WakeIntent negotiate BEFORE the extraction was wired into AuroraApp.tsx, then AuroraApp.tsx was rewired to use the hook and re-verified green. Two deliberate behavior-preservation design points: (1) bootstrap()'s original 'abort a stale/superseded bootstrap call before firing every other domain's initial fetch' race guard is preserved exactly via a resolveSession(isStale) callback, rather than being silently dropped by a naive parallel restructuring; (2) the original 23-way concurrent Promise.all in bootstrap() is preserved as a single Promise.all (the session/WakeIntent-return resolution still happens sequentially before it, exactly as before -- only the internal grouping of which fetches are 'hook-owned' changed, not the concurrency shape). Vitest 163/163 across 26 files (143/24 baseline + this checkpoint's 14 new useAuroraSession.test.ts cases + ~6 more from a concurrent B5 PWA checkpoint's own pwaManifest.test.ts, landed via commit 856a243 in the same working tree -- see discoveries). tsc -b and vite build both clean; the built src/main/resources/static/app/aurora/** already committed by that concurrent B5 checkpoint was confirmed byte-identical after rebuilding with this checkpoint's AuroraApp.tsx, i.e. the two checkpoints' work composes cleanly with zero interaction. Live-verified against the real running app (dev profile, H2 + Mock provider): register, mode-picker switch, a real multi-message Aurora reply mid-stream with the stop/interrupt control visible, clicking stop producing the exact original '已停在这里...' status copy, a second turn completing to 4 message bubbles, and WakeIntent negotiate producing a real return-card with the exact original '因为还有话没有说完...' copy and '约好了...' status. See evidence/track-b/README.md 'Workstream B1 -- fourth checkpoint' section."
commits:
  - sha: e176b61443192882b0a90bdbb7cbcd2ad3b3cff9
    summary: "docs(track-b): B0 experience observation of the live five-space AppShell"
  - sha: c2524d0
    summary: "fix(track-b): filter QA/service accounts out of People Discovery (pre-existing on branch at session start; recorded here since B0's status entry above predates it)"
  - sha: 93662ed
    summary: "feat(web): unify login/register into one AppShell entry surface (B1) -- AuthGate component, api.register, Aurora composer reorder; see evidence/track-b/README.md B1 section for the exact file list"
  - sha: cf5faac
    summary: "docs(track-b): resolve B0's open question on the 11 Resonance sample personas"
  - sha: 26f4aa2
    summary: "feat(web): introduce react-router (HashRouter) replacing the ?space= query param -- real per-space routes, legacy-link redirect, TB-REQ-002 filed for a future BrowserRouter swap"
  - sha: df2e189
    summary: "docs(track-b): draft v1 of the B7 8-12 minute demo script (concurrent with this checkpoint's own work; recorded here for a complete history since it landed on this branch between sessions)"
  - sha: a5f1e96
    summary: "feat(web): consolidate hand-rolled busy/error UI onto shared loading.tsx primitives (B1) -- nine components migrated onto AsyncButton/LoadingText; see evidence/track-b/README.md 'Workstream B1 -- third checkpoint' for the exact file list"
  - sha: 5e3790d
    summary: "docs(track-b): record the loading-audit checkpoint (B1) with evidence and next step"
  - sha: 856a243
    summary: "feat(web): add PWA offline app shell (B5 first slice) -- landed concurrently in the same checkout while this checkpoint's AuroraApp.tsx work was in progress; recorded here for a complete history since B5's own status entry above predates it having a commits: row"
  - sha: 24bea3c3e5f2566946cd80b1b3111b800df8d98d
    summary: "feat(web): extract Aurora conversation/session domain into web/src/hooks/useAuroraSession.ts (B1) -- first AuroraApp.tsx domain-hook decomposition slice; see evidence/track-b/README.md 'Workstream B1 -- fourth checkpoint' for the exact file list"
verification:
  B0_observation:
    method: "Live browser walk via Playwright (evidence/track-b/scripts/observe-b0-journeys.mjs), dev profile, H2 + Mock AI provider, no real keys."
    frontend_build: "cd web && npm ci && npm run build -- PASS, no rebuild committed (no source changes made, output was byte-identical to HEAD after line-ending normalization; discarded to keep the tree clean)."
    server: "./mvnw.cmd spring-boot:run under JAVA_HOME=jdk-21.0.10, reached http://localhost:8080/app/aurora/ with HTTP 200; /actuator/health reported DOWN only because of Redis health indicator (no Redis in dev profile) -- not a functional blocker, noted as a discovery below."
  B1_authgate_and_composer_reorder:
    method: "TDD: web/src/components/AuthGate.test.tsx written first (characterizes the pre-existing login-only behavior, then the new register-mode behavior), then the AuthGate component + api.register + AuroraApp.tsx wiring implemented to pass. Live-verified via evidence/track-b/scripts/observe-b1-register.mjs against a running dev-profile server, with the server restarted after each frontend rebuild (static assets are baked into the classpath at boot and are not hot-reloaded)."
    unit_tests: "cd web && npx vitest run -- 138/138 passing across 24 files, including the new 13-case web/src/components/AuthGate.test.tsx."
    build: "cd web && npm run build -- tsc -b && vite build, clean, output written to src/main/resources/static/app/aurora/."
    live: "dev profile, H2 + Mock provider. Fresh /app/aurora/ visit shows the login/register tablist; register-mode password mismatch shows an inline alert and does not call the API; successful register lands fully authenticated in the AppShell (five-space nav + composer visible); logout then re-login through the same toggle succeeds; expired/cleared-session deep link (?space=cosmos) now also offers the register tab; .composer bounding-rect is within the initial viewport (no scroll) on both 390x844 and 1440x900 after the JSX reorder; other four spaces (cosmos/resonance/letters/me) unaffected (spot-checked body text length + nav presence)."
  B1_loading_audit:
    method: "Read web/src/loading.tsx in full first to pin the exact API (useDelayedBusy 3-tier timing, AsyncButton, LoadingText, ConnectError), then grepped web/src/components/*.tsx and AuroraApp.tsx for hand-rolled busy/error patterns (a local busy boolean paired with manual disabled+ternary-text, or a raw busy-conditional block message). For each genuine match, checked its .test.tsx first; where a callsite's busy state had zero existing test coverage (UnderstandingCorrection's actual retire-confirm trigger), added a characterization test, ran it green against the pre-migration hand-rolled code first, then migrated and updated the assertion to match the new (correct) delayed-timing mechanism."
    unit_tests: "cd web && npx vitest run -- 143/143 passing across 24 files (was 142/142 before this checkpoint; +1 new characterization test in UnderstandingCorrection.test.tsx), run after every meaningful edit."
    build: "cd web && npm run build -- tsc -b && vite build, clean, run after the full migration set."
    live: "dev profile, H2 + Mock provider, server restarted after the rebuild. evidence/track-b/scripts/observe-b1-loading-audit.mjs intercepts POST /api/v1/auth/register and POST /api/psychology/skills/*/runs with an artificial ~1.8s delay (the local Mock/H2 backend otherwise resolves too fast to observe the delayed tiers) and confirms against the real running app: the register submit button keeps its original label and is already disabled at <1s with no flash, then shows '正在创建 ···' busy text at 1-3s; the psychology-skill-run button is disabled at <1s with its original label, then shows '正在整理 ····' busy text at 1-3s. Screenshots: evidence/track-b/screenshots/b1-loading-audit-01..05*.png."
  B1_aurora_session_hook_decomposition:
    method: "Read the whole of web/src/AuroraApp.tsx (1302 lines) first to precisely identify which of its 60+ useState/useRef/useCallback declarations belong to the Aurora conversation/session domain (message list, streaming/turn status, interrupt/stop, mode picker, WakeIntent negotiate, session bootstrap/replay) versus every other domain (memory/starfield, correction/claims, capsule/resonance, connections/letters, psychology skills, portrait, account) -- confirmed with a full-file grep pass for every candidate identifier before moving anything, not just from variable names. AuroraApp.tsx had zero prior test coverage of its own logic (only child components like AuroraConversation.tsx had prop-driven tests), so per this project's TDD norm a new web/src/hooks/useAuroraSession.test.ts was written FIRST against the not-yet-wired hook (renderHook from @testing-library/react + vi.mock('../api') mirroring AuthGate.test.tsx's mocking convention), covering: initial state, resolveSession (fresh session / WakeIntent-return session / stale-abort), replaceFromHistory, loadWakeIntents/loadNotifications, send + streamAurora event handling (turn.started/bubble.started/token/bubble.completed/turn.completed), stop/interrupt, setMode, and the four WakeIntent negotiate handlers (schedule/postpone/cancel/respond). Ran green against the hook in isolation (14/14) before wiring AuroraApp.tsx to call it, then re-ran the full suite and rebuilt after every meaningful edit."
    unit_tests: "cd web && npx vitest run -- 163/163 passing across 26 files (143/24 checkpoint-start baseline + this checkpoint's 14 new useAuroraSession.test.ts cases + ~6 more contributed by a concurrent B5 checkpoint's own pwaManifest.test.ts landed mid-session via commit 856a243 in the same working tree)."
    build: "cd web && npx tsc -b -- clean, zero errors. cd web && npm run build -- clean tsc -b && vite build; the resulting src/main/resources/static/app/aurora/** was byte-identical to what commit 856a243 (the concurrent B5 checkpoint) had already committed, confirming the two checkpoints' independent work composes with zero interaction once both were finished."
    live: "dev profile, H2 + Mock provider, .\\mvnw.cmd spring-boot:run under JAVA_HOME=jdk-21.0.10, server left running after the rebuild (static assets baked in at boot). evidence/track-b/scripts/observe-b1-decompose-aurora.mjs (run from a temporary copy under web/, per the by-now-standard Node ESM-resolver workaround) against a fresh registered account confirms: the mode picker's '整理' button gains the 'active' class on click; sending a message shows a real streamed Aurora reply mid-turn with the stop/interrupt control ('停止回应') visible (screenshot b1-decompose-02-mid-stream.png); clicking stop mid-stream produces the byte-exact original status copy '已停在这里。直接继续说，Aurora 会带着已听见的部分重新理解。' (screenshot b1-decompose-03-after-stop.png); a second full turn settles to 4 message bubbles in the conversation (screenshot b1-decompose-04-settled.png); WakeIntent negotiate (filling the '回来时间' input and clicking '和 Aurora 约好') produces a real return-card with the exact original '因为还有话没有说完...' reason copy and the exact original '约好了。你随时可以改期或取消，不需要迁就 Aurora。' status text (screenshot b1-decompose-05-wakeintent-scheduled.png). One methodology note logged as a discovery below: the very first live-verification attempt (before deliberately re-testing with Playwright's serviceWorkers:'block' context option) showed an apparent stream failure with no reproducible pattern across repeated runs with/without that option -- traced to the pre-existing, already-documented '403 then self-healing CSRF retry' behavior in api.ts's request() wrapper being occasionally visible as console noise on the very first POST right after a brand-new registration, not a regression from this extraction."
human_gates: []
blockers: []
discoveries:
  - "The React AppShell (/app/aurora/) has no registration form or register link at all; the only working registration entry point is the legacy static page /pages/register.html, completely unlinked from the new app. A user following CLAUDE.md's own run instructions hits a dead-end login form. This is the top-priority B1 fix."
  - "Registration -> AppShell session hand-off already works correctly: register.html redirects to /app/aurora/ and the cookie session carries over with no forced re-login."
  - "The Aurora conversation experience itself is already in good shape: live-verified multi-message pacing, a visible stop/interrupt control pair while streaming, and a dual-core status signal ('理解与表达双核协作'). Preserve this through the B1 refactor rather than rebuilding it."
  - "No real client routes exist anywhere; navigation is a single ?space= query param on one path (/app/aurora/), and all five spaces are simultaneously mounted in the DOM (toggled via the hidden attribute) out of one 1292-line AuroraApp.tsx with 60+ useState hooks. Confirms 对齐文档/20 4.3 with fresh evidence, not just re-citing the doc."
  - "Mobile bottom navigation is not fixed/sticky -- on the Aurora space it renders inline after the hero/mode-picker/WakeIntent/Self-Emergence blocks, pushing both the nav and the message composer below the fold on first load. Screenshot: evidence/track-b/screenshots/20-space-aurora-mobile.png."
  - "No PWA offline shell exists: forcing the browser fully offline and reloading produces a blank browser network-error page, not a branded offline state. Screenshot: evidence/track-b/screenshots/40-offline-reload-desktop.png."
  - "Expired/cleared-cookie deep links (e.g. ?space=cosmos) silently drop the intended target and show a bare, unexplained login form with no register link; user always lands back on the default aurora space after re-login. Screenshot: evidence/track-b/screenshots/41-expired-auth-desktop.png."
  - "The Resonance space ships 11 fully-written sample capsule personas (洛哥, 苏格拉底, 庄周, ...) visible to every brand-new, zero-activity account, grouped by all five target matching strategies. Whether this is intentional sample/practice content or leaked seed data is UNRESOLVED and should be a first product decision for B1/B2 before designing the Resonance first-run state."
  - "The People-Discovery list (Connections space) surfaces QA/test accounts to real users -- observed 'CSRF Runtime' x3, 'CSRF Check', 'Header', 'Smoke', and two of this pass's own throwaway B0-observer test accounts, all listed as people a brand-new real user could 'want to get to know'. Every registered account, including automated test ones, appears to be globally discoverable by default with no filtering. Needs a backend-side fix (track-b-integration-requests.yml candidate) before any local-complete demo is trusted."
  - "A shared loading/error primitive set already exists (web/src/loading.tsx: useDelayedBusy 3-tier timing, ConnectError, AsyncButton) implementing 对齐文档/11 14.3's loading-state rule correctly. B1's 'single async/task/error model' work should audit for consistent adoption across components rather than invent a new primitive from scratch."
  - "Every full navigation to /app/aurora/?space=<x> logs one console `403 POST /api/dialog/session/create` (confirmed the exact URL with a dedicated network-tap script). The conversation still works, so this reads as a redundant 'ensure session' bootstrap call re-firing on every full-page reload rather than a functional break -- silent to the user today, but worth Track A/B1 follow-up as part of consolidating bootstrap/session logic."
  - "Theme inconsistency: /pages/register.html and /app/aurora/ rendered in opposite time-of-day themes (light/day vs dark/night) at the identical wall-clock moment, because they are two separate implementations that don't share the same time-of-day computation."
  - "Portrait ('Aurora 眼中的你') is currently rendered inside the Me space, not Inner Cosmos, contradicting the target IA table in 对齐文档/09 5. Needs a deliberate decision in B1: move the component, or update the spec's target home for Portrait."
  - "B0's claim that the mobile bottom nav itself is not fixed/sticky did not reproduce this pass -- .app-shell-nav already has position:fixed at <=680px (confirmed live: position=fixed, anchored to the viewport bottom) and has for some time per git history (web/src/styles.css). Only the composer-below-the-fold half of that finding was real and is now fixed; the nav-not-fixed half of the original observation appears to have been a misread, not a regression introduced since B0."
  - "AuthController.register (/api/auth/register and /api/v1/auth/register, both mapped) already rotates the session id and stores the user id exactly like login does -- confirmed live, not just from reading AuthController.java -- so the new in-app register path could reuse the same onSuccess/bootstrap callback as login with no backend changes and no new integration request needed."
  - "The generated OpenAPI types (web/src/generated/inner-cosmos-v1.d.ts) have no RegisterRequest schema at all, unlike LoginRequest/ChatRequest/etc. api.register() therefore uses a small hand-written inline type rather than a CoreApiSchemas type, matching the one precedent of that pattern already in api.ts (draftSlowLetter's ad hoc body typing). Worth flagging to whoever owns the openapi-generate step as a documentation gap, though not filed as a track-b-integration-requests.yml item since it did not block this work."
  - "evidence/track-b/experience-inventory.md section 7's reproduction note ('script must run with cwd=web/') does not actually work in this Node 22.20 environment -- Node's ESM resolver walks up from the *importing file's own path*, not the process cwd, so a script physically under evidence/track-b/scripts/ can never resolve web/node_modules/playwright regardless of cwd. Confirmed by trying observe-b0-journeys.mjs itself (it also fails now). Workaround used this pass: run from a temporary copy placed under web/ (e.g. web/.scratch-e2e/), deleted afterward. Future sessions should either always use this workaround, or move the scripts under web/scripts/ permanently (not done this pass to avoid unrequested churn to evidence/track-b/scripts/'s existing layout)."
  - "Loading-audit checkpoint: nine components hand-rolled the exact busy/error pattern web/src/loading.tsx's AsyncButton/LoadingText already implement correctly (disabled+ternary-text buttons, or a raw busy-conditional block message with no delay tiering) -- see evidence/track-b/README.md 'Workstream B1 -- third checkpoint' for the full file-by-file list. No gap was found in loading.tsx itself; every instance mapped cleanly onto the existing API."
  - "A recurring, larger pattern was found and deliberately NOT fixed this pass: several components disable multiple SIBLING buttons (representing different candidates/proposals/versions/mode-tabs/strategies/runs/ratings) off of ONE SHARED busy boolean with no per-item id -- AuroraSelfSpace (propose/evaluate/activate/rollback across different items), MemoryStarfield's 时间/主题/人物 mode tabs, ResonanceNetwork's 5 strategy-switcher buttons, PsychologySkillStudio's per-run revoke button, CapsuleWorkbench's 5 sandbox-rating buttons. Wrapping each sibling in AsyncButton with the SAME shared busy flag would make every sibling show busy text the instant any one is clicked -- a UX regression, not a fix. These need a per-item busy id added to AuroraApp.tsx's state first (the same pattern already used for claimCandidateBusyId/detailBusy/rollbackBusy), which is domain-hook-decomposition-shaped work, not a call-site swap -- flagged as B1 backlog rather than force-migrated with a misleading mechanism."
  - "LettersInbox.tsx's reply-to-letter button has NO busy tracking passed in at all (no prop for it exists) -- clicking '让回复慢信启程' while the network call is in flight gives zero feedback and does not guard a double-submit. Real gap, not fixed this pass (needs new state threaded from AuroraApp.tsx's replyWithLetter, out of scope for a mechanism-only audit)."
  - "AuthGate's submit button previously disabled with ZERO busy feedback at all (no text change between idle and busy) despite being the very first interactive control any new user sees. Migrated to AsyncButton with a mode-dependent busyText ('正在登录'/'正在创建') following the '正在X' convention already established by every other AsyncButton call site in the codebase -- a small, convention-consistent copy addition, not an invented one-off."
  - "B5 PWA checkpoint: the app's existing bootstrap-error path (web/src/loading.tsx's ConnectError, already wired into AuroraApp.tsx's render for a failed bootstrap() call) turns out to ALREADY be the correct branded 'can't connect' UI for the offline case -- the only missing piece was the JS/CSS/HTML shell itself being loadable at all while offline. Once vite-plugin-pwa precaches that shell and a NavigationRoute falls back to it, the exact same ConnectError card renders offline with zero AuroraApp.tsx changes. Worth remembering for future B-track work: check whether a 'missing offline state' is actually a caching gap before assuming a new UI component is needed."
  - "The ConnectError card's detail line shows the raw browser error string verbatim (observed: literally 'Failed to fetch', in English, inside an otherwise fully Chinese UI) when bootstrap() fails from a real network-level failure (as opposed to a 401/business error, which already gets a translated message). This is a real, pre-existing small polish gap surfaced by making the offline path reachable for the first time -- not fixed this pass to avoid touching AuroraApp.tsx/api.ts while a parallel B1 agent is mid-refactor on AuroraApp.tsx. Candidate follow-up: detect network-level fetch failures (e.g. `error instanceof TypeError` or `!navigator.onLine`) in the bootstrap catch block and substitute a Chinese '你已离线' message instead of surfacing error.message directly."
  - "No sharp/ImageMagick/any image-processing dependency exists anywhere in this repo (checked: not in package.json, not on PATH -- Windows' own convert.exe at /c/Windows/system32/convert is the disk-format utility, not ImageMagick). The new web/scripts/generate-pwa-icons.mjs resizes the existing 1024x1024 app icon into the 192/512/512-maskable PWA sizes using a real <canvas> inside headless Chromium via the already-installed playwright devDependency, instead of adding a new native image dependency just for a one-time (re-runnable) icon export."
  - "vite-plugin-pwa's generated precache manifest (embedded in the built sw.js) lists each of the three public/icons/*.png files and manifest.webmanifest TWICE -- once from the plugin's own manifest.icons entries and once from the workbox globPatterns crawl of the same files in the dist output. Confirmed harmless (workbox's precache controller keys by URL internally, so this is redundant list entries, not double-cached storage or double network fetches) but left as a known cosmetic inefficiency rather than spending more time suppressing it via manifestTransforms for a first B5 slice."
  - "web/tsconfig.node.json previously had no explicit noEmit, and since it only covered vite.config.ts + playwright.config.ts (whose stray .js/.d.ts output at the web/ root is already covered by the root .gitignore's 'web/*.js' pattern), this never surfaced. Adding src/pwaManifest.ts to that project's file list (required so vite.config.ts can import the same tested buildPwaManifest() used by pwaManifest.test.ts) caused tsc -b to emit stray web/src/pwaManifest.js/.d.ts NOT covered by that gitignore pattern (which only matches direct children of web/, not web/src/). Fixed by adding 'noEmit: true' to tsconfig.node.json's compilerOptions -- worth remembering if a future checkpoint adds another src/ file to that project's include list."
  - "Confirmed live via a dedicated Playwright script (evidence/track-b/scripts/observe-b1-pwa-offline.mjs): after one online visit + a ~1.5s settle, the Cache Storage API reports exactly 14 precached entries and 0 of them are /api/ URLs; during a subsequent forced-offline reload, exactly one /api/ request fires (the bootstrap CSRF call) and Playwright's response.fromServiceWorker() reports false for it -- it fails on the network as expected, it is never served from any cache. This is the concrete evidence behind the 'never cache P0 API payloads' requirement in TRACK-B-COMPLETE-EXPERIENCE.md section 9, not just a config-reading assertion."
  - "A concurrent agent in the same checkout is mid-way through extracting Aurora-conversation-domain hooks out of AuroraApp.tsx (web/src/hooks/useAuroraSession.ts + .test.ts appeared during this session, and web/src/AuroraApp.tsx itself was observed in a transiently TS-broken intermediate state -- e.g. 'Cannot find name activeTurnRef/setMessages/finishTurn' -- confirmed via `npx tsc -b` producing errors ONLY inside AuroraApp.tsx and zero errors anywhere else, including every file this B5 checkpoint touched). Per this checkpoint's explicit brief, AuroraApp.tsx was left completely untouched; the PWA work does not depend on it being fixed. A full `npm run build` was verified clean twice earlier in this same session (before the other agent's later edit); the built src/main/resources/static/app/aurora/** committed here reflects that last-known-good build, already containing all of this checkpoint's PWA output (manifest.webmanifest, sw.js, icons/, etc.) -- whoever picks up B1's hook-extraction next should rerun `npm run build` once AuroraApp.tsx is consistent again, purely to confirm the two checkpoints' outputs still compose cleanly together (no PWA-specific action expected to be needed)."
  - "First domain-hook decomposition slice confirms the B5 discovery above and closes its loop: web/src/hooks/useAuroraSession.ts + useAuroraSession.test.ts (14 cases) is exactly the work the B5 checkpoint observed in-flight; AuroraApp.tsx was left in a fully consistent, green (tsc/vitest/build) state by the time B5's commit 856a243 landed, and rebuilding after B5's commit reproduced its already-committed src/main/resources/static/app/aurora/** byte-for-byte -- the two independent checkpoints compose with zero interaction, exactly as B5's discovery predicted."
  - "AuroraApp.tsx's bootstrap() originally guarded against a superseded/stale bootstrap call (relevant to mobile OIDC re-init racing a previous in-flight bootstrap) by checking a call-token BEFORE firing its one big 23-way Promise.all of every domain's initial fetch -- if stale, it returned without ever touching setSessionId or any of those 23 fetches. Naively splitting 'resolve which session to load' into the new hook while keeping 'load everything else' in AuroraApp.tsx would have broken this guard (the two halves would run in parallel via a wrapping Promise.all, so a stale call would still fire all 20 other-domain fetches before finding out it was stale). Fixed by having the hook's resolveSession(isStale) accept a callback and check it itself, right before setSessionId, exactly mirroring the original single-function control flow -- AuroraApp.tsx's bootstrap() still does ONE sequential 'resolve session' step followed by ONE 23-way Promise.all, unchanged in shape from before this extraction."
  - "Live-verification methodology note: the very first live-verification pass (default Playwright context, service worker allowed to register since a B5 concurrent checkpoint just landed) showed an apparent streamAurora failure with no stop control ever appearing and the status stuck at '还没建立回应时间线，请重试这句话。' A second run with the same context ('serviceWorkers: block' Playwright option) succeeded cleanly, and a THIRD run re-testing with service workers allowed again also succeeded cleanly -- i.e. the failure did not reproduce on repeat regardless of the service-worker setting, so it was NOT conclusively caused by the new service worker (that would have been an unproven assertion). The real, already-documented explanation is api.ts's request() wrapper: it retries once on a 403 CSRF_INVALID response, and a handful of POSTs right after a brand-new registration (stream-stage, psychology/skills/suggestions, wake-intents/negotiate, turns/:id/stop) intermittently hit this retry-then-succeed path, visible as console 403 noise even though the actual call succeeds on retry -- consistent with the pre-existing 'redundant 403 POST /api/dialog/session/create' discovery logged earlier in this file. Every clean run (screenshots b1-decompose-02..05) shows the exact original status copy and message counts, confirming this extraction changed nothing observable."
handoff_notes:
  - "B0 is observation-only per the task charter -- nothing above was fixed in this pass. golden-journeys.md's 'What this unblocks for B1' section is the prioritized starting backlog for the next session: (1) unify the login/register entry surface, (2) reorder the mobile Aurora space so the composer is reachable without scrolling past capability displays, (3) resolve what the 11 Resonance personas are, (4) get a dev-safe way to fast-forward WakeIntent arrival so J2 can be driven end-to-end, (5) filter test/service accounts out of People Discovery, (6) build a real offline app shell."
  - "J2 (WakeIntent longitudinal return) and most of J3 (capsule compile/sandbox/persona-chat/slow-letter send) could not be live-verified in a single observation pass without either waiting real wall-clock time or fabricating account history that would need to be cleaned up before a trusted demo. Treat every UNVERIFIED row in golden-journeys.md as real backlog, not as 'probably fine'."
  - "evidence/track-b/scripts/observe-b0-journeys.mjs is reusable for future checkpoints -- rerun it after any B1/B2 change to re-screenshot the same five spaces and compare before/after."
  - "This checkpoint closed B0 backlog items (1) and half of (2) from the list above: unified login/register entry surface, and mobile+desktop composer reorder ahead of WakeIntent/Self-Emergence. Remaining B0 backlog items are still open and unclaimed: (3) resolve what the 11 Resonance personas are, (4) a dev-safe way to fast-forward WakeIntent arrival for J2, (6) a real offline app shell (B5). Item (5), filtering test/service accounts out of People Discovery, appears already done on this branch (web/src/testAccountFilter.ts, commit c2524d0) even though the B0 status entry predates it -- worth a quick confirmation pass rather than re-doing it."
  - "Next unblocked B1 step, in priority order per the spec's suggested route model (docs/tracks/TRACK-B-COMPLETE-EXPERIENCE.md section 5): introduce react-router (or equivalent) so /today, /aurora/:sessionId?, /cosmos/starfield, /resonance/discover etc. become real addressable routes instead of the single ?space= query param -- this is the prerequisite for shareable deep links (e.g. a specific star, capsule, or letter thread) and for splitting AuroraApp.tsx's 60+ useState hooks into per-route domain hooks. A second, independent, similarly-scoped next step: audit which components still hand-roll busy/error UI instead of adopting web/src/loading.tsx's existing AsyncButton/ConnectError/LoadingText primitives (the 'single async/task/error model' requirement) -- this can be done incrementally, one component at a time, without waiting for the routing migration."
  - "Not attempted this pass (explicitly out of scope per the task brief): retiring register.html/login.html, resolving the 11 Resonance personas' provenance, WakeIntent fast-forward for J2, and the PWA offline shell. All remain open B1+/B5 backlog."
  - "Real-routing checkpoint: the implementing sub-agent was cut off mid-task by an account-level API session limit (not a code/test failure) after implementation + unit tests (142/142) + build were already green, but before live verification/commit. The lead session picked up the already-clean, already-tested working tree, ran live verification itself, and completed the checkpoint -- recorded here for continuity since the handoff happened mid-workstream rather than at a clean agent boundary."
  - "The 11 Resonance personas' provenance question (open since B0) is now resolved: source-confirmed via SeedCapsuleContent.java's own Javadoc ('Official seed EchoCapsules. These are product-designed agents, not user clones.') that they are intentional, permanent showcase content. See evidence/track-b/discoveries/resonance-persona-provenance.md (commit cf5faac) for the full decision and its B2 implication (add an 'official sample capsule' provenance badge rather than hiding/removing them)."
  - "HashRouter was the correct choice, not a shortcut: confirmed live that Spring only forwards the exact /app/aurora and /app/aurora/ paths (no /app/aurora/** catch-all), so a BrowserRouter path would 404 on refresh. Filed TB-REQ-002 for the future server-side fallback + BrowserRouter swap; the app code depends only on react-router's location/navigate API so the swap will be a one-line change in main.tsx when the backend side lands."
  - "Playwright verification methodology note: getByRole('button', {name}) with exact:false performs substring matching against the FULL accessible name (all descendant text, e.g. a nav button's <strong>label</strong><small>description</small> both count) across the ENTIRE page, not just an intended container -- since all five product spaces are simultaneously mounted (hidden-toggled, not unmounted) per the existing architecture, an unscoped/loose name match can silently grab a same-substring element from a different, currently-hidden-looking-but-still-matched space. Scope nav-button locators to page.getByRole('navigation', {name: ...}) first, and use each space's exact label from ProductShell.tsx's productSpaces array ('我的' not '我', '今天' not 'Aurora') -- see evidence/track-b/scripts/observe-b1-routes.mjs for the corrected pattern."
  - "Loading-audit checkpoint done: nine components (MemoryStarfield, ClaimCandidateReview, CapsuleWorkbench, UnderstandingCorrection, PsychologySkillStudio, RelationsView, AuthGate, AuroraConversation, PortraitView) migrated onto web/src/loading.tsx's existing AsyncButton/LoadingText primitives; loading.tsx itself needed no changes. This closes the 'single async/task/error model' item from the earlier handoff note above, at least for the unambiguous single-button/single-item cases -- the remaining shared-busy-across-siblings cases (see discoveries) are real but smaller follow-ups, not blockers."
  - "Next unblo